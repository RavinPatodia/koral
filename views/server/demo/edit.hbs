<div class="row">
    <div class="col-md-12">
        <p class="lead">
            <a href="{{adminDir}}/demo/" class="btn btn-default btn-sm">demo列表</a>
            <a href="{{adminDir}}/demo/add" class="btn btn-default btn-sm">添加demo</a>
        </p>
        <form action="" method="post">
            <div class="form-group">
                <label>名称</label>
                <input name="name" value="{{demo.name}}" class="form-control">
            </div>
            <div class="form-group">
                <label>描述</label>
                <textarea name="description" rows="8" class="form-control">{{demo.description}}</textarea>
            </div>
            <button type="submit" class="btn btn-default">提交</button>
        </form>
    </div>
</div>
